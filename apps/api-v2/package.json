{
  "name": "@contan2/api-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -b",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint placeholder (eslint config llega en PR siguiente)'"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "zod": "^3.24.0",
    "@contan2/config": "workspace:*",
    "@contan2/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.10.0"
  }
}
