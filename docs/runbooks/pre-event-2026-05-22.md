# Pre-evento 2026-05-22 — Centro Cultural Banreservas

**Operación:** evento real con visitantes del CCB el **viernes 22 de mayo de 2026**.
**Modo:** conservador. Solo lectura sobre código de admin esta semana.
**Responsable de la operación:** Marcelino Francisco Martínez.
**Asistente operativo en standby:** Claude.

---

## 1. Punto de referencia conocido bueno (capturado 2026-05-18)

### Código en producción

| Item | Valor |
|---|---|
| Branch desplegada | `multitenant` |
| Commit local HEAD | `4c62e92c9f6900b6f00b68e44b18827bfd179356` |
| Commit local (corto) | `4c62e92` |
| Mensaje commit | `feat(responsive): hardening global — todas las vistas responsive end-to-end` |
| Fecha commit | 2026-05-15 16:15:45 -0700 |
| Sync con `origin/multitenant` | ✅ Local y origen coinciden |
| Próximo SHA esperado en prod | El mismo, no hay commits pendientes |

> **Si durante el evento alguien introduce un commit nuevo a `multitenant` sin
> aviso, hay que revertir a `4c62e92` antes de cualquier diagnóstico.**
> Este commit es el "último estado conocido bueno".

### Infraestructura (Coolify)

| Item | Valor |
|---|---|
| Coolify API URL | `http://217.77.12.180:8000` |
| App UUID | `f3xck8spocf0o377y9w0vq6n` |
| App name | `contan2-saas-app` |
| URL pública | `https://ccb.contan2.com` |
| Estado actual app | `running:healthy` |
| `restart_count` actual | `0` (sin reinicios recientes) |
| `last_online_at` | `2026-05-18 14:49:19 UTC` |
| `config_hash` | `7bc096042195f9ef014095ec2574d34b` |
| Build pack | `dockerfile` |

### Volumen persistente

| Item | Valor |
|---|---|
| Storage UUID | `qaudnl3q0hixfmmnl3mql7hl` |
| Host path (VPS) | `/data/contan2/uploads` |
| Mount path (container) | `/app/backend/data/uploads` |
| Tipo | bind mount, `is_directory: true` |

### Versiones runtime

| Item | Valor |
|---|---|
| Imagen base | `node:20-bookworm-slim` (Node 20 LTS) |
| `engines.node` declarado | `>=18` (satisface 20) |
| Cliente Postgres en runtime | `pg ^8.20.0` |
| Postgres server | gestionado por Coolify (versión no expuesta vía API; verificar en panel de Coolify si se requiere) |
| Sharp | `^0.34.5` (con Chromium instalado vía apt para Puppeteer) |
| Puppeteer | `puppeteer-core ^24.43.1`, Chromium del sistema |
| Resend | `^6.12.3` |

### Tenant / branding del CCB

```json
{
  "slug": "ccb",
  "name": "Centro Cultural Banreservas",
  "logoUrl": "/uploads/1778861352720-bf8e54ab45e6.svg",
  "primaryColor": "#0182a2",
  "secondaryColor": "#ff6f00",
  "sidebarStyle": "brand",
  "codePrefix": "CCB"
}
```

### SSL

| Item | Valor |
|---|---|
| Emisor | Let's Encrypt (vía Traefik + Coolify) |
| Válido desde | 13 may 2026 21:32 UTC |
| Válido hasta | **11 ago 2026 21:32 UTC** |
| Días al vencimiento | ~85 días (sin riesgo en el evento) |

---

## 2. Smoke test del flujo del evento — RESULTADOS (2026-05-18)

> **Decisión operativa:** no se crea visitante "Test Smoke" porque la
> única actividad pública activa es la del evento real (Los Congos de
> Villa Mella, 22 may). Crear un visitante de prueba inflaría el
> conteo y dejaría basura en la actividad real.
> En su lugar, se usa el código `CCB-VPGUCM` (Marcelino Francisco M.,
> ya existente como user del kiosko con email
> `mfranciscomartinez@gmail.com`) para verificar credencial + email.
> No se crea ni borra nada → cero riesgo de contaminar la operación.

| # | Caso | Resultado | Detalle |
|---|---|---|---|
| 2.1 | `GET /healthz` | ✅ 200 | `{ok:true,ts:"2026-05-18T14:56:17.669Z"}` |
| 2.2 | `GET /api/_tenant` | ✅ 200 | Branding completo: slug ccb, primary #0182a2, secondary #ff6f00, sidebarStyle brand, codePrefix CCB, logoUrl correcto |
| 2.3 | `GET /kiosko` | ✅ 200 | HTML 2810b con `<style data-branding-ssr>` inyectado (10 stops del primario derivados de #0182a2) y 2 elementos con `data-org-logo` (img + fallback) |
| 2.4 | Crear visitante prueba | ⏭️ Saltado | Marcelino ya tiene user `CCB-VPGUCM`; no se crea para evitar contaminar la actividad real. Lookup público confirma 1 match para prefix "mfranc" |
| 2.5 | `GET /api/credentials/CCB-VPGUCM.png` | ✅ 200 | PNG válido (header `89 50 4e 47`), 37 345 bytes, 900×560 px, 8-bit colormap |
| 2.6 | `POST /api/credentials/CCB-VPGUCM/send` | ✅ 200 + ⏳ humana | Resend ID `de0a86be-b69a-4f1e-b7f1-bb3e145b510d`, destinatario `mfranciscomartinez@gmail.com`. **Marcelino debe confirmar recepción en su bandeja.** |
| 2.7 | `POST /api/staff/login` (PIN) + `/api/staff/me` | ✅ 200 | Login con PIN 2828 OK, cookie expira `2026-05-19T03:05:23Z` (~12h TTL), `/me` autenticado |
| 2.8 | `GET /api/reports/activity/<congos>.xlsx` | ✅ 200 | 15 861 bytes, `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`, Excel 2007+ válido |
| 2.9 | `GET /api/reports/activity/<congos>.pdf` | ✅ 200 | 175 359 bytes, PDF-1.4, 1 página |
| 2.10 | Limpieza | ✅ N/A | Nada que limpiar (no se creó nada nuevo) |

### Veredicto del smoke

**El sistema está operacional y listo para el evento del viernes.**

Sin bugs bloqueantes detectados. Cosas menores observadas se anotan en
`pre-event-known-issues.md`. Performance:
- `/healthz` ~350ms
- Credencial PNG 1.6s (incluye render sharp + carga de logo SVG embed)
- Email send 1.7s
- Reportes Excel/PDF dentro del rango habitual.

---

## Confirmaciones humanas pendientes

| Item | Quién confirma | Estado |
|---|---|---|
| Recepción del email de credencial de prueba en `mfranciscomartinez@gmail.com` | Marcelino | ⏳ esperando |
| Horario exacto del evento del viernes 22 may | Marcelino | ⏳ esperando |
| Backup `pg_dump` ejecutado el jueves 21 may en la noche | Marcelino | ⏳ pendiente |
