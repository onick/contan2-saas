node_modules
**/node_modules
.env
**/.env
.env.local
.env.production
*.log
.DS_Store
.vscode
.idea

# Agent skills (project-local, no se commitean al repo)
.agents/
.claude/
skills-lock.json

backend/data/db.json
backend/data/db.json.tmp
backend/data/uploads/*
!backend/data/uploads/.gitkeep
backups/*
!backups/.gitkeep
_design-explorations/

# Release script · secretos + evidencia local de deploys
.env.release
release-evidence/

# Monorepo v2 build artifacts (mismas reglas que migration/saas-platform-v2-parallel,
# acá también para que `git status` esté limpio cuando los artefactos viajan
# entre branches en el mismo working tree)
.turbo/
**/.turbo/
**/dist/
**/*.tsbuildinfo
